return function()
  -- semehow its not working here, so i move that to ui.lua
    -- vim.g.sonokai_style = "shusia"
    -- vim.g.sonokai_enable_italic = 1
    -- vim.g.sonokai_better_performance = 1
    -- vim.g.sonokai_transparent_background = 1
end
